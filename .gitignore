# dependencies
node_modules/

# build output
.next/
# Alternate build dirs, for a dev server that must not share one with a build.
.next-*/
out/

# env files — never commit secrets
.env
.env.local
.env.*.local

# vercel
.vercel

# misc
.DS_Store
*.pem

# git worktrees
.worktrees/

# typescript incremental build cache
*.tsbuildinfo
